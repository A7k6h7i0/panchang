name: panchang_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.13.1
  cupertino_icons: ^1.0.8
  flutter_compass: ^0.8.1
  permission_handler: ^12.0.1
  geolocator: ^14.0.2
  alarm: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
